spring:
  datasource:
    url: jdbc:h2:mem:vantagepoint_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

application:
  security:
    jwt:
      secret-key: "esta-es-una-clave-secreta-de-prueba-de-al-menos-32-caracteres"
      expiration: 3600000